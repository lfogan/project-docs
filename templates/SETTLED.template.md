# SETTLED - {{PROJECT_NAME}}

<!-- Owner rejections and findings withdrawn as wrong - never choices
     (artifacts record choices). Grep this before raising audit findings or
     re-proposing a change.
     Grammar (lint-checked), one line per entry:
       - Don't <imperative> - settled YYYY-MM-DD (<one-clause why>)
     Delete a line only when the owner reopens the topic. -->
