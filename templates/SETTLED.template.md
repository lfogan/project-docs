# SETTLED - {{PROJECT_NAME}}

<!-- The record of "no" - the one thing git cannot hold (git records what was
     done, never what was declined). A line lands here ONLY when the owner
     rejects a proposal or a finding is withdrawn as wrong. Never to log a
     choice: artifacts (design, code, tests) record choices.
     Grammar, lint-checked, one line per entry:
       - Don't <imperative> - settled YYYY-MM-DD (<one-clause why>)
     Example:
       - Don't re-raise "no interruptions" on the jobs footer - settled 2026-08-16 (owner rejected the change)
     Delete a line only when the owner reopens that topic. -->
