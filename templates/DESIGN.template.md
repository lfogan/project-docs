# DESIGN - {{PROJECT_NAME}}

<!-- Single source of truth for design and copy. Owner-approved edits only
     (R3): agents propose a diff and stop; landing needs [design-approved] in
     the commit message. Current design only - overwrite old text, git keeps
     history. No deviation log, no history sections. Assets in this directory
     share the gate. If the shipped product disagrees with this file, raise
     it - never silently pick a side. -->

{{DESIGN_SEED}}
