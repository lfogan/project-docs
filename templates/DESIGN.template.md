# DESIGN — {{PROJECT_NAME}}

<!-- The single source of truth for design and copy. Owner-approved edits only
     (R3): agents propose a diff and stop; landing an approved change needs
     [design-approved] in the commit message. Describes the CURRENT design
     only — when a decision changes it, overwrite the old text; git holds
     every previous version. No deviation log, no history sections, no
     "superseded" markers. Assets (mockups, handoff files, art) live beside
     this file in docs/design/ under the same gate. If the shipped product
     disagrees with this file, one of them is wrong — raise it, never
     silently pick a side. -->

{{DESIGN_SEED}}
