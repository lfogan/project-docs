# DONE - {{PROJECT_NAME}}

<!-- Frozen archive of completed task rows. Append-only, newest at the bottom,
     NEVER read wholesale and never auto-loaded - grep it when you need to
     know whether something was already attempted.

     Rows arrive here VERBATIM from TODO.md, one line each, at the moment they
     are marked [done]. Do not rewrite, summarise, group, or enrich them on
     the way in. In particular: no What/Why/Evidence/Limits blocks and no
     multi-line entries - the lint rejects both, because that shape is exactly
     how a task list turns into a changelog nobody reads.

     Nothing is ever written directly into this file. A row arrives only by
     being marked [done] in TODO.md and moved here - if it was not a task, it
     does not belong, however interesting it was.

     The story behind a task is in its commit; the code is in the tree. This
     file answers one question only: was this task done, and roughly when.
     Never rotate, trim, or summarise it. It is meant to accumulate. -->
