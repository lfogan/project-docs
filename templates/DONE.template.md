# DONE - {{PROJECT_NAME}}

<!-- Append-only archive, newest at the bottom; never auto-read - grep it to
     check whether something was already done.
     Rows arrive VERBATIM from TODO.md, one line each: no rewriting, no
     What/Why/Evidence/Limits blocks, max 300 chars per line (lint-checked).
     Never write here directly: a line that was not a TODO row does not
     belong. Never rotate, trim, or summarise this file. -->
